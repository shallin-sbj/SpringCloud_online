eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8520/eureka/
management:
  metrics:
    web:
      server:
        auto-time-requests: false


server:
  port: 9411
spring:
  application:
    name: zipkin-service
  main:
    allow-bean-definition-overriding: true
